These configuration files were ported over from the original Blitz project.
We need to determine whether they are still required in this form for, say, Rio-based deployment
(which uses Groovy-based configuration).